# Five packages, and nothing pinned harder than a floor. This project has no build step
# and no compiled dependency, so a fresh virtual environment is the whole setup.

streamlit>=1.50   # width="stretch" and the height rules below need 1.50 or newer
duckdb>=1.1
pandas>=2.0
openpyxl>=3.1
pytest>=8.0
